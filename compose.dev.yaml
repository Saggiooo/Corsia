# docker compose -f compose.yaml -f compose.dev.yaml up
services:
  web:
    build:
      target: tools
    command: npm run dev
    environment:
      NODE_ENV: development
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
